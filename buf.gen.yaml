version: v2
clean: true
plugins:
  # Go message types
  - local: protoc-gen-go
    out: gen
    opt: paths=source_relative
  # Go ConnectRPC service handlers + clients
  - local: protoc-gen-connect-go
    out: gen
    opt: paths=source_relative
